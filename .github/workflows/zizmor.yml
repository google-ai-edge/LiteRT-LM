# zizmor: ignore[concurrency-limits]
# zizmor: ignore[template-injection]
name: Zizmor

# zizmor: ignore[concurrency-limits]
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read # requires permission

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  zizmor:
    name: Zizmor workflow security analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write # requires permission
      contents: read # requires permission
      actions: read # requires permission
    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Install rust
        run: rustup default stable

      - name: Install Zizmor
        run: cargo install zizmor --version 1.28.0 --locked

      - name: Run Zizmor
        run: zizmor --format sarif . > results.sarif
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@f3712979fa5f215279b101dd0a2e3bdfb4353324 # v3
        with:
          sarif_file: results.sarif
          category: zizmor
